@page
@model FuturionNewsfeed.Pages.News.IndexModel

@{
    ViewData["Title"] = "Hírfolyam";
}

<h2>Hírfolyam</h2>

<p>
    @if (User.Identity.IsAuthenticated)
    {
    <a asp-page="Create">Új hír felvétele</a>
    }
</p>
<hr />

@*<table class="table">
        <thead>
            <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.NewsItem[0].Title)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.NewsItem[0].Text)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.NewsItem[0].CreationDateTime)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.NewsItem[0].CreatorUsername)
                    </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
    @foreach (var item in Model.NewsItem) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Text)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CreationDateTime)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CreatorUsername)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
    }
        </tbody>
    </table>*@

@foreach (var item in Model.NewsItem.Reverse())
{
<div>
    
        <h3>&gt;&gt;<b>@Html.DisplayFor(m => item.Title)</b></h3>
        <h4>&emsp;&emsp;@Html.DisplayFor(m => item.ShortenedText)...</h4>
        <span>
            <i>
                &emsp;&emsp;&emsp;Létrehozó: @Html.DisplayFor(m => item.CreatorUsername)<br />
                &emsp;&emsp;&emsp;Létrehozás dátuma: @Html.DisplayFor(m => item.CreationDateTime)<br />
                &emsp;&emsp;&emsp;Megjegyzések: @Html.DisplayFor(m => item.NumberOfComments)<br />
            </i>
        </span><br />
        <a asp-page="./Details" asp-route-id="@item.Id">
    Hír megtekintése
            </a><br />
</div>
<hr/>

}